# 骨盆局部前后倾：M1–M7 小规模修正与对照实施方案

日期：2026-09-18；2026-09-19 更新。状态：`S1_DEVELOPMENT_PARTIAL`。

> 执行顺序更新：本文件后文原 A/B/C/D 设计已由用户确认的逐级 S1–S6 顺序取代，后文仅保留为历史设计与文献依据，不是当前执行协议。先完整测试 S1 相对角单目标；只有观察到对应问题时，依次新增 S2 骨盆世界角、S3 胸廓保持、S4 接触、S5a 轨迹、S5b 时间连续性、S6 显式接触恢复。不得预先堆叠保护项。2026-09-19 的实际开发结果及未完成项见 `result.md` 开头；旧历史结果与独立确认集保持不变。

本方案响应用户同意尝试局部相对角引导、并结合文献实施多条解决路线的要求。保留旧协议、旧结果和独立确认集；新增协议 `vimogen_local_pelvis_pilot_v1`，不修改历史 C0 的语义。参数清单见 `configs/m1_m7/local_pelvis_pilot_v1.json`；该文件记录首轮来源与阶段计划，S1 实际参数和结果应以运行记录、`result.md` 为准，不能把草案字段当作已完成实验。

## 一、结论与实验问题

推荐主线是“校正前倾符号 + 骨盆—胸廓相对角 + 世界姿态/接触保护”，并以“解析协同补偿 + 足部逆运动学”为输出空间参照。不是简单把正负号交换，也不是仅把旧角度换成两条曲线相减。

已有诊断确认：sample94/seed0 的旧 M7 `+10°` 实际使校正骨盆标志角改变约 `−9.99995°`，躯干前倾改变 `−10°`；M7 的 56 条冻结记录中，局部关节旋转改变为零，根局部关节位置 P95 最大仅约 `0.000239 mm`。它实现的是整体旋转。证据在 `diagnostics/pelvis_sign_rigid_lean_20260918/`。

需要逐项回答：

1. 修正定义后，正剂量是否确实让骨盆前侧下沉？
2. 相对角达标是否来自骨盆转动，还是躯干反向弯曲？
3. 上身稳定后，脚是否滑动、踮起或穿地？
4. 输出空间有可行解时，冻结生成模型能否通过引导得到同类解？
5. 接触恢复放在生成末端还是生成过程中，哪一种更有效、代价多大？

历史限制必须继承：此前 v1.3 的脊柱补偿已提示“上身稳住不等于脚稳住”；v2.1 源噪声相对角单例在既定预算内只达到约 `+2.046°`，最后回退 M0，不能视为成功或通过增加同类试验次数绕过。新三维相对角与旧 v2.1 固定矢状面定义不同，数值不可直接合并。

## 二、文献依据与可借鉴的边界

本次为面向实现的定向检索，不是系统综述。没有可调用的专用学术检索连接器，使用技能提供的 OpenAlex 后备脚本发现条目，再通过会议官方页面、作者原文和 arXiv 核对；按标题/作者合并会议版与预印本，不以引用数筛选。

| 文献 | 已核对内容 | 本项目采用什么；不据此声称什么 |
| --- | --- | --- |
| Xie 等，OmniControl，ICLR 2024，[官方全文](https://proceedings.iclr.cc/paper_files/paper/2024/file/7827290f07f63485b849b66cfa3e5dd0-Paper-Conference.pdf) | 空间控制与学习式真实性引导互补；后者可协同调整全身关节 | 首轮借鉴多部位联合约束；不声称只加损失就复现其学习式模块，也不假定其已验证骨盆局部角任务 |
| Karunratanakul 等，Optimizing Diffusion Noise Can Serve As Universal Motion Priors，CVPR 2024，[作者全文](https://arxiv.org/html/2312.11994v2) | 将输出空间目标反传经过完整去噪过程，优化噪声，用于动作编辑和保持内容 | 支持 M2 的源噪声路线；本项目既有失败提示先检查梯度、信赖域与可达性，不保证任何姿态目标都可达 |
| Ben-Hamu 等，D-Flow，ICML 2024，[官方记录](https://proceedings.mlr.press/v235/ben-hamu24a.html) | 通过流模型反传，优化源点，实现受控生成 | 借鉴可微完整采样；论文展示的图像、音频、分子任务不构成本项目骨盆控制的直接证据 |
| Huang 等，StableMoFusion，2024 预印本，[原文 §4.3/附录 B](https://arxiv.org/html/2405.05691v1) | 足接触、姿态、轨迹和地面反作用力相关项用于滑步清理，并将清理嵌入去噪 | 借鉴接触锚点与生成中修正；首轮不使用其训练过的受力预测模块，不把纯运动学修正称为受力正确 |
| Yuan 等，PhysDiff，ICCV 2023，[作者原文入口](https://arxiv.org/abs/2212.02500) | 通过物理仿真中的动作模仿策略进行生成过程投影 | 支持后续物理投影方向；需要仿真器和策略，不是当前项目可直接调用的普通逆运动学函数 |
| Tevet 等，CLoSD，ICLR 2025，[作者全文](https://xbpeng.github.io/projects/CLoSD/CLoSD_2025.pdf) | 动作规划器与强化学习跟踪控制器形成闭环 | 可作为动力学闭环备选，首轮不引入这套训练与系统成本 |

检索词包括 `OmniControl motion control`、`D-Flow controlled generation`、`Optimizing Diffusion Noise motion priors`、`PhysDiff physics guided motion`、`StableMoFusion foot contact`、`CLoSD simulation diffusion`。OpenAlex 查询命中 OmniControl 的 arXiv 条目；最终按官方会议版计一篇。CVF 页面/全文在部分打开请求中返回内部错误，DNO 转用作者 arXiv 全文、PhysDiff 转用作者摘要入口；不声称精读所有论文全文。

以下算法组合、阈值和排程是本项目的工程设计，不是论文已证明的结论。

## 三、先固定可检验的控制定义

### 3.1 语义与坐标

正剂量表示“在原动作基础上，骨盆前侧相对后侧进一步下沉”。剂量是相对共享 M0 的逐帧增量，而非把所有帧变成同一个绝对角。体型、帧率、动作长度和地面保持一致。

新建校准文件，不覆盖历史 `pelvis_landmarks_v4.json`。核验前/后、左/右及竖直轴；标志点与鼻尖朝向交叉检查，并在网格侧视图叠加前后标志点。鼻尖只作朝向校验，不能代替骨盆解剖标志。虚拟刚性标志轴属于模型代理量，不称为临床测量的真实骨性角度。

### 3.2 主目标：三维骨盆—胸廓相对角

用权威局部旋转经正向运动学得到骨盆和胸廓的世界旋转，分别乘固定解剖校准矩阵，记为 `R_P` 与 `R_T`。胸廓默认用 spine3 的世界旋转，另以肩线、spine1→neck 方向作独立评价，不能从骨盆朝向复制胸廓坐标。

令 `p_T = R_T^T R_P e_front`，在胸廓解剖矢状面中定义：

`theta_rel = -atan2(p_T · e_up, p_T · e_front)`。

新目标为 `wrap(theta_rel(candidate) − theta_rel(M0)) = dose`。骨盆—左/右股骨相对角另外报告，不强制二者都改变相同剂量，以免破坏原有步态摆腿。

整个身体同乘任意旋转 Q 时，`(Q R_T)^T(Q R_P)=R_T^T R_P`，所以此指标不奖励整体倾倒。纯根旋转对它的导数应为零。这正是 M3/M4 旧 `d(phi)/d(theta_root)=1` 不能沿用的原因。不能把两个世界欧拉角简单相减作为通用三维实现。

对投影长度接近零、角度分支跨越或缺少有效帧返回明确无效状态，不以零填充。内部计算用弧度；报告用度。使用角差环绕并检查跨帧连续性。

### 3.3 防止“弯腰代替骨盆动”

同时计算校正骨盆标志轴在 M0 冻结水平朝向与世界竖直组成的平面内的前侧下沉角 `theta_world`。完整方案要求其增量也接近 dose，并约束胸廓/肩线世界姿态相对 M0 的改变。

原因是相对角本身并不辨别谁在动：骨盆不动而胸廓反转，同样能改变相对角。世界角与相对角的“双目标”加胸廓保护，才对应本项目所需的局部编辑。大幅转体或侧屈时两个标量未必严格可同时满足，按三维可行性检查处理，不强求相减恒等式。

## 四、共同实施的四组方案

| 编号 | 操作 | 范围 | 实验角色 |
| --- | --- | --- | --- |
| A：相对角单目标 | 用 theta_rel 替换旧根代理角；保留每种方法必要的数值稳定项和配对内容正则 | M1–M7 的新版变体 | 最小消融，观察弯腰取巧、角度欠响应；不是默认最终方案 |
| B：相对角 + 姿态/接触保护 | A 加 theta_world 目标、胸廓保护、M0 冻结接触、轨迹及时间平滑约束 | M1–M7 的新版变体 | 推荐主线；允许必要的腰髋协调，不允许整个人跟着倒 |
| C：解析协同补偿 + 足部恢复 | 骨盆转动；spine1 和双髋子节点逆补偿；最后全序列下肢逆运动学恢复足底锚点 | 独立 M7-C 输出空间参照 | 验证运动学可行性，分开保存补偿前/后与逆运动学前/后 |
| D：生成中接触投影 | 对 B 的最佳两种生成方法，将同一接触投影器放到后期若干生成步；另做仅末端投影对照 | B 通过几何冒烟后执行，最多两种方法 | 区分“生成过程形成协调”与“末端修好了画面” |

A 与 B 的算子、初始噪声和基本预算成对一致，差别仅在约束包。C 不是 M1–M6 的生成能力证明。D 必须保留 B 原输出、B+末端投影、B+生成中投影三个结果，不能把投影后的收益全归于原方法。

### 4.1 B 的残差与变量

统一返回有物理单位的逐帧残差，再按对应容差归一化：相对角、骨盆世界角、胸廓方向、足底锚点、地面穿透、根轨迹和修正量时间差分。目标角使用等式残差；保护项只惩罚超出容差的部分。每种算法采用其自身的梯度、对偶或投影更新，最终都由同一个独立评价器验收；软损失变小不等于硬门通过。

直接姿态修正的初始变量集为根旋转/平移、spine1–3、双髋、膝、踝和足关节；肩肘腕不直接编辑。生成模型后续仍可能改变其他关节，必须评价，不能把编辑掩码误称为保持保证。M2 优化源噪声，变量空间与直接姿态法不相同，单独记录，不假装自由度完全相等。

角度/接触约束可能冲突，使用阻尼和信赖域、小步迭代并在每次步长裁剪后重新计算残差。若没有可行点，报告 `INFEASIBLE_WITHIN_BUDGET` 及最佳不可行点，而不是放松门槛或把回退 M0 计为成功。

### 4.2 C 的可复现构造

1. 对每帧 M0，用校准矢状轴求让骨盆前侧下沉 dose 的根旋转 `R_new`，以双髋中点为旋转中心补偿根平移。
2. 对根的三个直接子节点 `c ∈ {spine1, left_hip, right_hip}`，设 `L_c_new = R_new^T R_old L_c_old`。在刚性骨架上可保持这些子树的世界朝向，而不是只抵消胸廓。
3. 子树朝向不变并不保证子节点位置不变：旋转后的骨盆偏置仍会移动关节中心。必须先量化，再用根平移和双腿关节的全序列逆运动学恢复接触。姿态形变后的网格也不能假定完全刚性。
4. 接触时段由 M0 一次性确定，不根据候选重算以逃避检查。平足时用脚跟/前掌多个锚点；自然脚尖支撑时只约束真实接触区域，不把全部步态强行锁成平足。摆动期不锁脚。
5. 锚点首轮跟随 M0 对应帧位置，以检查“不额外破坏接触”；另报告绝对滑动，不能把 M0 原有滑步当自然动作。跨帧平滑优化修正量，避免逐帧逆运动学造成抖动。

C 的无接触恢复版本作为机制消融。只有 C 在核心剂量上存在可行解，才把相同目标送回生成引导作性能比较。单次优化失败只能说明预算内未找到解，不能证明人体任务不可行。

### 4.3 D 的首轮实现

冻结采样器和 50 步设置，以“距终点剩余 10、5、1 个更新步”定义三次插入点，避免混淆不同实现的 sigma 方向。在预测干净动作上做同一个约束投影，并通过各方法已有的端点回传/速度修正接口返回采样状态；不能把物理姿态直接覆盖到噪声状态。每次最多 3 次阻尼更新，末端对照给相同累计 9 次更新预算。记录额外正向运动学次数、网络求值次数和耗时；迭代数相同仍不等于成本相同。

首轮不改 ViMoGen 权重，不新增受力预测网络或强化学习控制器。

## 五、M1–M7 的具体接入点

| 方法 | 必须修改的内容 | 特殊验收 |
| --- | --- | --- |
| M1 能量梯度引导 | 新版共享几何残差；A/B 使用相同更新规则，B 加规范化保护残差 | 梯度方向、掩码、归一化前后单位一致 |
| M2 源噪声优化 | 对完整采样输出计算 A/B 目标；保留源噪声信赖域和原始噪声 | 先解决现有批处理/单样本一致性阻塞；未通过不得计有效 M2。先做 8 次优化冒烟，最多 32 次诊断预算，不直接重复 120 次旧实验 |
| M3 局部投影 | 替换根单自由度解析雅可比，改为上述多关节变量上的阻尼雅可比求解 | 根单变量对相对角导数为零；检查秩、裁剪后的真实约束残差 |
| M4 端点/轨迹修正 | 同样扩展变量和雅可比；先保留末端版，再由 D 测生成中版 | 末端-only 版本单独标注，不能描述为全过程生成引导 |
| M5 对偶约束引导 | 新目标与逐项保护残差；对每个约束组维护缩放和对偶量 | 输出每项违反量及对偶量，防止某一尺度吞没其余约束 |
| M6 Lyapunov 引导 | 新复合误差及信赖域；记录实际误差下降与约束违反 | 不将标量能量下降当作所有硬门通过 |
| M7 几何编辑参照 | 新版 A/B 为输出空间局部姿态约束求解；另列 C 的解析初始化与接触恢复 | 明确这是扩展后的 M7 变体，不覆盖历史 root-only 实现；生成后处理成本单列 |

当前公共配置虽列有 C1–C3，方法实现多处明确拒绝非 C0。新任务需要新的请求/残差接口和版本化分发，不能仅修改 YAML 后宣称已支持。

计划新增模块（以下尚未实现）：

- `geometry/local_pelvis_v1.py`：校准坐标、世界/相对角、有效性与可微残差。
- `guidance/local_pelvis_constraints_v1.py`：A/B 约束包、冻结 M0 锚点与单位缩放。
- `guidance/pelvis_contact_projector_v1.py`：解析补偿、阻尼多关节投影、全序列接触恢复。
- `evaluation/local_pelvis_metrics_v1.py`：独立三维/标志点验收，不直接复用损失作为唯一真值。
- `experiments/run_local_pelvis_pilot_v1.py`：预检、缓存哈希、计划行、状态与结果归档。
- 对应测试：符号、刚体不变性、有限差分雅可比、零剂量恒等、接触时段冻结、不可行/无有效接触状态、批处理一致性。

所有权威输入仍为 body_pose/root_rotation/root_translation；更新后统一正向运动学重建冗余位置与速度通道。禁止分别“修好”评价用关节和渲染用旋转。

## 六、阶段、样本量与停止条件

### P0：几何与表示预检，不跑生成扫描

人工构造正负 5°/10°，检查前侧下沉；任意整体旋转相对角应不变；仅胸廓反转应触发世界骨盆目标失败；C 子树逆补偿应保持世界旋转。有限差分核验自动梯度，校验编码/解码/渲染轴链。零剂量直接返回逐位相同的 M0。

失败立即停止，不允许通过调引导强度修复定义错误。

### P1：输出空间参照

现有开发样本 94、34122，seed0，七档剂量 `−10/−5/−2/0/+2/+5/+10°`。C 无接触恢复与有接触恢复两版，共 28 个配置输出。两动作是已有直行/转弯压力样例，不宣称代表动作总体。

先看 ±2°/±5°；±10°为压力测试。核心剂量无法同时满足角度、胸廓和接触门时，先报告约束冲突及每项残差，不进行大规模 M1–M7 参数搜索。几何空间可行与源噪声空间可达性必须分别判断。

### P2：M1–M7 接口冒烟

sample94/seed0，`−5/0/+5°`，A/B 两组，再加 C 参照：最多 `7×2×3+3=45` 个配置输出；M2 若仍阻塞则为 39 个执行槽位、6 个阻塞槽位，不能在表里悄悄删除。每方法/约束包仅用一套预先记录配置。

M2 的“8 次更新”是新冒烟上限，不是沿用旧 v2.1 的 RMS 预算；新旧噪声 L2/RMS 要同时输出并注明维度，避免把不同信赖域数值直接比较。先测源噪声到相对角的雅可比和有限差分一致性；输出空间可行但源空间无改善时，保留负结果。

### P3：小规模正式对照，不接触独立确认集

P0/P1 通过后，使用 2 动作 × 2 种子（0、42）× 7 剂量，共 28 个配对条件。A/B 的七种方法加 C 为最多 `28×15=420` 个配置输出；M2 若阻塞则 364 个执行槽位、56 个阻塞槽位。零剂量属于恒等性审计，不需要重复采样。

每方法/约束包最多允许 P2 中记录的两套配置作开发比较，P3 只使用选定的一套，不把 420 再乘多轮参数网格。只有代码、配置、基线/噪声哈希完全相同的 P1/P2 产物才可复用到 P3，否则重新运行并分别记录。

### P4：D 的插入位置对照

按 P3 的核心剂量全门通过数、接触退化、成本的固定顺序选最多两种 M1–M6 方法；A 不参与候选优选。每个方法做末端投影、生成中投影两版，每版 28 条，最多新增 112 条；未投影的 B 从 P3 复用。各版独立报告，不隐藏无效、超时或失败行。

如 B 无通过者，但 C 已可行，只允许选残差最小的最多两种方法做“挽救性探索”，单独标注，不能称为胜出者复验。若 C 也未过门，停在运动学/约束诊断，不进入此阶段。

P3/P4 完成后才决定是否恢复原有 8 动作开发扩样；4 动作独立确认集继续封存。首轮不使用少数动作估计总体 FID 或声称统计显著性。

## 七、统一验收与输出

下列是待冻结的工程初值，不是临床标准或论文通用阈值。在 P0/P1 的明确校准阶段可以修订并升版；P3 开始前冻结，之后不因看见失败而放宽。

| 项目 | 初步门槛/规则 |
| --- | --- |
| 正负号 | 每个非零剂量的骨盆世界角平均增量同号；按剂量分开报告，不用正负平均相抵 |
| 角度控制 | B/C/D 的世界骨盆角及相对角：MAE≤1°，P95≤2°；A 也按完整门评价，并另报单目标命中 |
| 胸廓稳定 | 胸廓完整世界旋转测地差、spine1→neck 方向差的 P95 均≤2°；另报有符号前后倾 |
| 双足方向 | 支撑期脚跟→前掌方向三维变化 P95≤3°；完整足坐标旋转另外报告，不能把一条方向冒充足部完整姿态 |
| 接触保持 | M0 冻结接触标志点相对对应帧位移 P95≤10 mm；平足/脚尖支撑分开评估 |
| 穿地 | 足底标志点绝对穿透 P95≤5 mm，且相对 M0 增量≤1 mm；同时报告最大值/异常帧率 |
| 滑动 | 接触期滑动速度 P95 相对 M0 增量≤max(5%, 5 mm/s)；另报原始绝对速度和整段滑移，不把“未恶化”写成“无滑步” |
| 根轨迹 | 配对根平移偏差 P95≤20 mm；保留绕髋中点修正所需的小平移，不硬锁根位置造成不可行 |
| 时间连续性 | 骨盆/胸廓角度“修正量”的相邻有效帧变化 P95≤1°/帧（20 fps），不同帧率换算；其他关节角速度/加速度、末端突变和限位作独立审查 |
| 零剂量 | 权威动作逐位恒等；评价/网格误差允许仅由既定数值重建产生，单独审计 |
| 数据有效性 | NaN、无有效接触、几何退化、缺少完整网格评价均不得自动计通过；返回 NOT_EVALUABLE 或失败原因 |

完整通过还需要通过已有骨架限位/表示一致性检查及侧视视频审查；数值门不是“自然”的充分证明。既有 M0 本身不满足绝对质量时，保留 `BASELINE_QUALITY_FAIL` 标签，分开报告相对保持与绝对质量，不悄悄剔除该动作。滑动仅通过相对门不意味着物理平衡成立。

输出每个配对条件的机器状态、输入/配置/代码哈希、M0/原始候选/修正候选、逐帧与整段指标、失败原因、网络求值/反传/正向运动学次数、耗时和显存。回退 M0 单列为失败；同时保存最佳不可行候选用于解释原因。

视频使用一致地面与尺度、M0 冻结的侧视相机，不逐候选重定向或独立自动扶正。叠加骨盆前后标志、胸廓轴、左右足接触标志、目标剂量、实际相对角和世界角。转弯样本额外保留固定世界视角。主比较为 `M0 | A | B | C`；D 的三版本另排。先判断姿态和接触，再检查曲线，防止只被指标误导。

## 八、运行与工程交付次序

1. 先实现并服务器测试新几何/校准，不动历史入口；固定新协议哈希。
2. 实现 C 与接触投影器，运行 P1；形成可行性报告与带标志侧视视频。
3. 新建 A/B 请求接口，逐方法接入；先 M7、M1，再 M3/M4、M5/M6；M2 单独解除一致性阻塞。执行 P2。
4. 冻结配置，执行 P3，再按规则启动 P4。先统计通过/失败/不可评价和成本，再排序，不只挑好看视频。
5. 只有出现跨两动作/两种子的核心剂量稳定阳性，才恢复扩样；压力剂量单独给能力边界。

动态测试和实验仍通过主项目 `connect_server.py` 在服务器执行；本地默认 Python 缺少 PyTorch。服务器目录沿用 `/root/autodl-tmp/vimogen_m1_m7_scale`，Python 沿用 `/root/miniconda3/envs/mdm5090/bin/python`，开始前重新核验运行资源、模型与 M0/噪声缓存。新产物另建 `results/phase9/pelvis_m1_m7/local_pelvis_pilot_v1/attempt_*`，不得覆盖旧尝试。凭据只经运行时安全输入，禁止存入协议或报告。

不预报 GPU 小时数：先用 P2 实测，再按各方法中位耗时估算 P3/P4。最多 420 个 P3 槽位不代表现在已经启动这些任务。

## 九、无训练路线不足时的备选

若 C 能通过，但 B/D 长期欠响应：用通过全门的 C 结果构建训练集，训练冻结 ViMoGen 旁路的小型全身残差适配器，输入原动作、剂量、接触时段，输出根/腰髋/下肢修正；加入零剂量恒等、剂量符号、接触和时间平滑约束。按动作划分训练/验证，不能用独立确认集制作教师数据。教师数据仅运动学可行，不自动等于真实动作分布。该路线需另定训练数据和预算，不混入“零训练 M1–M7”表。

若角度和运动学接触均好，但仍有重心/支撑与动态平衡问题：再引入 PhysDiff/CLoSD 启发的物理跟踪或投影，明确需要角色映射、仿真器和策略适配。行走中的质心投影不必始终落在瞬时支撑多边形内，首轮不能把简单静态平衡条件当通用硬门。

最终推荐顺序：先证明 C 的可行性，以 B 为生成主线、A 为最小消融，随后用 D 验证生成中协调是否真正有收益。训练适配器与物理仿真是有触发条件的后续路线，而不是未经验证就同时启动的大工程。

## 本次交付状态

已完成代码接口与历史结果核查、定向文献检索和上述实施设计，并新增机器可读协议草案。尚未实现新版算法、运行 P0–P4、启动服务器实验或产生新的成功率；不得把本文件引用为新实验结果。
